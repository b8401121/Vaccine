[package]
name = "vaccine-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["wasmbind"] }
wasm-bindgen = "0.2"
serde-wasm-bindgen = "0.6"
